from setuptools import find_packages, setup

setup(name='Work Sample for Data Engineer',
      version='0.1',
      author='Amir Ghassemi',
      packages=find_packages(),
      include_package_data=True,
      description='',
      requires=[])
